syntax = "proto3";

package metrics;

message Metric {
  string deviceId = 1;
  double temperature = 2;
  double humidity = 3;
  string timestamp = 4;
}

service MetricService {
  rpc sendMetric (Metric) returns (Ack);
}

message Ack {
  bool success = 1;
}
